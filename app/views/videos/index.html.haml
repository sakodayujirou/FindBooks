.index__wrapper
  =render "sidebar"
  .right
    = image_tag 'bird.jpg',class:"background"
    .img__cover
      .text
        %h2 FindBooks
        %p 〜 オススメの本を動画で共有しあうプラットフォーム〜
    .content-wrapper
      %p.title あなたへのおすすめ
      -@videos.each do |video|
        .content
          =link_to "/videos/#{video.id}" do
            .content__image
              =  image_tag video.image.url,class: "index__image" if video.image.present?
            .content__title
              %p.top
                =video.title.truncate(27,omission:"...")
              %p.bottom
                %span 投稿者 :
                = video.user.name
-# .search__bottom_wrapper
-#   .check_box_title
-#     ジャンルで探す
-#   .search_bottom_1
-#     = form_tag("/videos/genre",method: :get) do
-#       =collection_check_boxes :genre,:keywords,Genre.all,:name,:name
-#       =submit_tag 'Search',class:"saerch__bottun"
-# =link_to "本の一覧", "/genres",class:"book__list__link"
=render "footer"
