.history__wrapper
  .history
    .profit__title
      利益表
    -@profits.each_with_index do |profit, id|
      .histories
        = id+1
        = profit.book.title.truncate(14,omission:"...")
        .created_at
          = profit.created_at.strftime("%Y/%m/%d %H:%M")
        .price
          = profit.price
          円
      
    .total
      .total__price
        =@total__price
        円
      .total__title
        総売上額

  -#     .history__wrapper
  -# .history
  -#   .history__title
  -#     購入履歴
  -#   -@histories.each_with_index do |history, id|
  -#     .histories
  -#       = id+1
  -#       = history.book.title.truncate(14,omission:"...")
  -#       .created_at
  -#         = history.created_at.strftime("%Y/%m/%d %H:%M")
  -#       .price
  -#         = history.price
  -#         円
  